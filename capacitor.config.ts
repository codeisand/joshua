import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'codeisand.joshua',
  appName: 'joshua',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
